#!/Melify/bin/mac/tclkit

if {$argc != 1} {
    puts "\nUSAGE: bab <javascript-file-name>\n"
    exit
}

set fname [file rootname [lindex $argv 0]]

exec /usr/local/bin/babel $fname.js --out-file $fname.es6 --presets=/usr/local/lib/node_modules/babel-preset-es2016
