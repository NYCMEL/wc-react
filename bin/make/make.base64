#!/Melify/bin/mac/tclkit

if {$argc == 0} {
    puts "\nUSAGE: make.base64 <file-name>"
    exit
}

# CONVERT PMG TO BASE64 (GET THEM ON A SINGLE LINE)
set b64 [exec openssl enc -base64 -in [lindex $argv 0]];regsub -all {\n} $b64 "" b64

puts $b64
