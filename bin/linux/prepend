#!/Melify/bin/mac/tclkit

set fname [lindex $argv 0]
set str   [lrange $argv 1 end]

exec /bin/echo "$str" > /tmp/cat.txt
exec /bin/cat /tmp/cat.txt $fname > /tmp/cat.txt

# can now do the rename/move
exec mv cat.txt $fname
